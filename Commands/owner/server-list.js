const Discord = require("discord.js")
const db = require("quick.db")
const fs = require('fs')
const owner = "729007378602655854"
module.exports = {
    info: {
        name: 'serverlist',
        cat : [""],
        description: '',
        coolDown: 1
    },
    run: async (message, args, prefix, client) => {
const cmd = message.content.split(" ")


 
  
                                    
        } 
  
}